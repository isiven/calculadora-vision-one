# Anthropic API key used by serverless functions in api/
ANTHROPIC_API_KEY=

# Optional local development notes:
# - Do not commit .env files with real values.
# - Configure the same variable in Vercel project settings for production.
